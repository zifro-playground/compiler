using System;

namespace ErrorHandler
{
	public enum ErrorType
	{
		LogicOrder,
		ForLoop,
		WhileLoop,
		Variable,
		Function,
		IfStatements,
		ElseStatements,
		Expression,
		Indentation,
		Text,
		Number,
		Keyword,
		Other
	}
}

